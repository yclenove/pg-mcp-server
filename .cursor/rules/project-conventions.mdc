---
description: 项目开发约定：Git提交规范、安全规范、代码风格
alwaysApply: true
---

# Git 提交规范

- Commit message 必须使用中文
- 每完成一个阶段的修改并测试通过后，自动 commit + push
- 禁止将数据库密码、API Key 等敏感信息提交到 Git（包括 .env 文件、测试脚本中的硬编码密码）
- 测试用的数据库连接信息只能通过 .env 文件或环境变量传入，不能写入源码

# 安全规范

- .env 文件已在 .gitignore 中，不得移除
- 测试脚本中若需数据库连接，从环境变量读取，不得硬编码
- 代码审查时重点关注是否有敏感信息泄露

# 更新日志

- 每次功能更新、Bug 修复、优化等变更都必须在 CHANGELOG.md 中记录
- 格式遵循 Keep a Changelog 规范（https://keepachangelog.com/zh-CN/）
- 分类使用：新增(Added)、变更(Changed)、修复(Fixed)、移除(Removed)、优化(Optimized)
- 日志条目使用中文编写

# MCP 与 Token

- 修改 Tools / Resources / Prompts 时须节约 LLM token，详见 `AGENTS.md` 与 `mcp-token-economy.mdc`

# 功能与单测

- 新增或变更 `src/` 行为时须配套 `test/*.test.mjs` 与 `package.json` 的 `test` 列表等，细则见同目录 `feature-with-tests.mdc`。

# 代码风格

- 使用 TypeScript strict 模式
- 使用 Prettier 格式化（详见 .prettierrc）
- 使用 ESLint 检查（详见 eslint.config.js）
